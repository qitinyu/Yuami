---
import BaseLayout from '../../layouts/BaseLayout.astro';
import SidebarWidget from '../../components/SidebarWidget.astro';
import { friendsList } from '../../data/friends';
import { fetchAllFriendArticles } from '../../utils/rss-parser';

// 从友链 RSS feed 获取最新文章（构建时执行）
const friendsArticles = await fetchAllFriendArticles(friendsList, 3);

const hasArticles = friendsArticles.length > 0;
---

<BaseLayout title="朋友圈">
  <div class="page-layout">
    <SidebarWidget />

    <div class="page-main">
      <div class="circle-header">
        <h2 class="circle-title"><i class="fa-solid fa-rss"></i> 朋友圈</h2>
        <p class="circle-desc">来自友链朋友们的最新文章动态，每次构建时更新。</p>
      </div>

      {hasArticles ? (
        <div class="articles-list">
          {friendsArticles.map((article, index) => (
            <article class="article-item card" style={`animation-delay: ${index * 0.06}s`}>
              <div class="article-item-header">
                <div class="article-author">
                  <img src={article.avatar} alt={article.name} class="author-avatar" onerror="this.style.display='none';this.nextElementSibling.style.display='flex';" />
                  <div class="author-avatar-placeholder" style="display:none;">
                    <i class="fa-solid fa-user"></i>
                  </div>
                  <a href={article.url} target="_blank" rel="noopener" class="author-name">{article.name}</a>
                </div>
                <span class="article-date">{article.date}</span>
              </div>
              <div class="article-item-body">
                <a href={article.articleUrl} target="_blank" rel="noopener" class="article-item-title">{article.title}</a>
                {article.excerpt && <p class="article-item-excerpt">{article.excerpt}</p>}
              </div>
            </article>
          ))}
        </div>
      ) : (
        <div class="circle-empty">
          <i class="fa-solid fa-rss"></i>
          <p>暂未获取到友链文章</p>
          <p class="empty-hint">请确保友链数据中已填写 rss 字段，或等待下次构建更新。</p>
        </div>
      )}

      <div class="circle-notice">
        <i class="fa-solid fa-circle-info"></i>
        <span>想加入朋友圈？请前往 <a href="/friends">友链</a> 页面申请。</span>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .page-layout {
    max-width: var(--max-width);
    margin: 0 auto;
    display: flex;
    gap: 24px;
    padding: 28px 24px;
  }

  .page-main {
    flex: 1;
    min-width: 0;
  }

  .circle-header {
    margin-bottom: 24px;
  }

  .circle-title {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 6px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .circle-title i {
    color: var(--primary);
  }

  .circle-desc {
    font-size: 0.92rem;
    color: var(--text-muted);
  }

  .articles-list {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .article-item {
    padding: 18px;
    animation: fadeInUp 0.5s ease both;
  }

  .article-item-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
  }

  .article-author {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .author-avatar {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    object-fit: cover;
    border: 1px solid var(--primary-alpha-30);
  }

  .author-avatar-placeholder {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--primary-alpha);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--primary);
    font-size: 0.7rem;
  }

  .author-name {
    font-size: 0.88rem;
    font-weight: 500;
    color: var(--primary);
  }

  .author-name:hover {
    text-decoration: underline;
  }

  .article-date {
    font-size: 0.8rem;
    color: var(--text-muted);
  }

  .article-item-body {
    padding-left: 36px;
  }

  .article-item-title {
    display: block;
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 6px;
    cursor: pointer;
    transition: color var(--transition-fast);
  }

  .article-item-title:hover {
    color: var(--primary);
  }

  .article-item-excerpt {
    font-size: 0.88rem;
    color: var(--text-muted);
    line-height: 1.6;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* 空状态 */
  .circle-empty {
    text-align: center;
    padding: 48px 24px;
    background: var(--bg-card);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-color);
  }

  .circle-empty i {
    font-size: 2rem;
    color: var(--primary-alpha-30);
    margin-bottom: 12px;
  }

  .circle-empty p {
    color: var(--text-muted);
    font-size: 0.95rem;
  }

  .circle-empty .empty-hint {
    font-size: 0.82rem;
    margin-top: 4px;
    color: var(--text-muted);
    opacity: 0.7;
  }

  .circle-notice {
    margin-top: 24px;
    padding: 12px 16px;
    background: var(--primary-alpha);
    border-radius: var(--radius-sm);
    font-size: 0.88rem;
    color: var(--text-secondary);
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .circle-notice i {
    color: var(--primary);
  }

  .circle-notice a {
    color: var(--primary);
    font-weight: 500;
  }

  @media (max-width: 1024px) {
    .page-layout { flex-direction: column-reverse; }
  }
</style>
